/**
 * The inbuilt default levels of logging supported.
 */
enum LoggingLevel {
    Trace = 0,
    Debug = 10,
    Info = 20,
    Warn = 30,
    Error = 40,
    Silent = 100
}

export default LoggingLevel;